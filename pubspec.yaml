name: lazydb
description: A sample command-line application.
version: 0.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  nocterm: ^0.7.0
  path: ^1.9.0
  postgres: ^3.5.12
  riverpod: ^3.3.2
  riverpod_annotation: ^4.0.3

dev_dependencies:
  build_runner: ^2.15.0
  lints: ^6.0.0
  nocterm_lints: ^0.3.0+beta.1
  test: ^1.25.6
